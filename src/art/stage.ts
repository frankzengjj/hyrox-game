/** Vertical layout shared by the side-view scenes (px). */
export const STAGE = {
  /** Where athletes' soles touch the floor. */
  floorY: 440,
  crowdTop: 188,
  barrierTop: 352,
  /** Back edge of the floor, where the barrier meets it. */
  floorTop: 400,
} as const;
