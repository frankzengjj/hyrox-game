/** Race seconds that pass per real second, so a ~75 min race plays in ~12 min. */
export const TIME_SCALE = 6;

export const RUN_COUNT = 8;
export const RUN_DISTANCE_M = 1000;
export const LAPS_PER_RUN = 4;
export const ROXZONE_DISTANCE_M = 50;
